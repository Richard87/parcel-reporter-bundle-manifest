{
  "name": "test-parcel",
  "private": true,
  "scripts": {
    "build": "parcel build src/index.html --public-url http://localhost:8080/build"
  },
  "devDependencies": {
    "parcel-reporter-bundle-manifest": "file:../../.."
  }
}
